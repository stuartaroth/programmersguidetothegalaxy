booleanValue = true

if booleanValue
  # if boolean_value is true
else
  # if false

intValue = 0

if intValue >= 1
  # if intValue is greater than or equalt to 2
else if intValue < 0
  # if intValue is less than 0
else
  # if none of the options

stringValue = "tea"

if stringValue is "tea"
  # if stringValue is "tea"
else if stringValue is "almost, but not quite, entirely unlike tea"
  # if stringValue is "almost, but not quite, entirely unlike tea"
else
  # if none of the options

if not booleanValue and stringValue is "tea"
  # if booleanValue is false and stringValue is "tea"
else if booleanValue or intValue is 0
  # if booleanValue is true or intValue is equal to 0
else
  # if none of the options
