# Nim does not support ternary operators

# Nim supports if statements written on one line

var trueValue = true
var falseValue = false

var trueIfResult = if trueValue: "trueResult" else: "falseResult"
# "trueResult"

var falseIfResult = if falseValue: "trueResult" else: "falseResult"
# "falseResult"
