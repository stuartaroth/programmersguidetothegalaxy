trueValue = true
falseValue = false

trueIfResult = if trueValue then "trueResult" else "falseResult"
# "trueResult"

falseIfResult = if falseValue then "trueResult" else "falseResult"
# "falseResult"
